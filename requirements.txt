# Core dependencies
python-dotenv==1.0.0
pydantic>=2.6.0,<3.0.0
pydantic-settings>=2.1.0,<3.0.0

# API Framework
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.9

# NeMo Guardrails
nemoguardrails==0.10.0

# OpenAI SDK with Agents support
openai>=1.54.0

# LangGraph / LangChain (model-agnostic agent runtime)
# Pinned to be compatible with nemoguardrails 0.10.0 (requires langchain-core<0.3.0)
langgraph>=0.1.0,<0.2.0
langchain-core>=0.2.14,<0.3.0
langchain-openai>=0.1.0,<0.2.0
langchain-anthropic>=0.1.0,<0.2.0
langchain-community>=0.2.0,<0.3.0

# Data storage
redis==5.0.1
asyncpg==0.29.0
sqlalchemy==2.0.25
alembic==1.13.1

# Observability
structlog==24.1.0
prometheus-client==0.19.0

# Security & validation
pyjwt==2.8.0
cryptography==42.0.0

# HTTP client with proxy support
httpx==0.26.0

# Optional: RAG/embeddings (if needed)
# sentence-transformers==2.3.1
# pgvector==0.2.4
